import { SiteLoadingScreen } from "@/components/site-loading";

export default function RegisterLoading() {
  return (
    <SiteLoadingScreen
      message="Preparing registration"
      submessage="We will have you in the workspace in a moment."
    />
  );
}
