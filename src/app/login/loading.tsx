import { SiteLoadingScreen } from "@/components/site-loading";

export default function LoginLoading() {
  return (
    <SiteLoadingScreen
      message="Opening sign-in"
      submessage="Secure session, calm interface."
    />
  );
}
