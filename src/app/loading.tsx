import { SiteLoadingScreen } from "@/components/site-loading";

export default function Loading() {
  return (
    <SiteLoadingScreen
      message="Loading"
      submessage="Polishing the concierge experience."
    />
  );
}
